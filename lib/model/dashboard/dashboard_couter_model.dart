import 'package:flutter/cupertino.dart';

class DashboardCounter {
  String title, count;
  IconData icon;
  Color color;

  DashboardCounter(
      {required this.title,
      required this.icon,
      required this.count,
      required this.color});
}
